#include<stdio.h>
#include<strings.h>
int main(){
    // char str[] = "College Wallah";
    // puts(str);
   char  str[5];
   puts(str);
    gets(str);
    printf("Your input is : %s",str);
   return 0;
}